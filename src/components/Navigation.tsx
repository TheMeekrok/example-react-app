import * as React from 'react';
import { Link } from 'react-router-dom';

function Navigation() {
    return (
        <nav className='h-[50px] flex justify-between bg-gray-500 items-center text-white'>
            <span className='font-bold ml-2'>React 2022</span>

            <span>

            </span>
        </nav>
    );
}

export default Navigation;